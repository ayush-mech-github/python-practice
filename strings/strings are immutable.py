# once string is declared, it cannot be changed. It can be modified only.
name = 'ayush'
# name[0]='A'
print(name.replace('a', 'A'))  # it will modify the original string.
new_name = name.replace('a', 'A')
print(new_name)
